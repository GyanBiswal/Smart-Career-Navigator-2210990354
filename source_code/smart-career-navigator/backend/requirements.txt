# Smart Career Navigator – Backend Dependencies
# -----------------------------------------------
# Install with:  pip install -r requirements.txt
#
# Note: spaCy language model must be downloaded separately after install:
#   python -m spacy download en_core_web_sm

# Web framework
flask==3.0.3
flask-cors==4.0.1

# NLP
spacy==3.7.4

# Sentence embeddings (BERT-based)
sentence-transformers==3.0.1

# Numerical computing
numpy==1.26.4

# MongoDB driver (optional – used if you wire up DB persistence)
pymongo==4.8.0

# Production WSGI server (optional – use instead of Flask dev server)
gunicorn==22.0.0
